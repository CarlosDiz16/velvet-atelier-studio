<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Velvet Atelier | Everlasting Art</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400&family=Playfair+Display:ital,wght@0,400;1,400&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #F5F5F0; 
            --text-color: #2D2D2D; 
            --accent: #C5B358; 
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Montserrat', sans-serif;
            scroll-behavior: smooth;
        }

        /* Nav */
        nav {
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            letter-spacing: 2px;
            font-size: 0.7rem;
            position: sticky;
            top: 0;
            background: rgba(245, 245, 240, 0.9);
            z-index: 100;
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            text-transform: uppercase;
        }

        /* Hero */
        header {
            height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 8vw, 4.5rem);
            font-weight: 400;
            margin: 0.5rem 0;
            font-style: italic;
        }

        .subtitle {
            text-transform: uppercase;
            letter-spacing: 6px;
            font-size: 0.8rem;
            opacity: 0.7;
        }

        /* Catalog */
        .section-label {
            text-align: center;
            padding: 4rem 0 2rem;
            font-size: 0.7rem;
            letter-spacing: 4px;
            text-transform: uppercase;
            border-top: 1px solid #E0E0DB;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 4rem;
            padding: 0 10% 5rem;
        }

        .product-card {
            text-align: center;
        }

        .product-image {
            width: 100%;
            aspect-ratio: 4/5;
            background-color: #EBEBE6; /* Placeholder for your photos */
            margin-bottom: 1.5rem;
            transition: opacity 0.3s;
        }

        .product-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        .product-price {
            font-size: 0.8rem;
            opacity: 0.6;
            letter-spacing: 1px;
        }

        /* Commission Form */
        .form-container {
            max-width: 600px;
            margin: 0 auto 8rem;
            padding: 0 5%;
        }

        input, select, textarea {
            width: 100%;
            padding: 1rem 0;
            margin-bottom: 2rem;
            border: none;
            border-bottom: 1px solid #CCC;
            background: transparent;
            font-family: 'Montserrat', sans-serif;
            outline: none;
        }

        .submit-btn {
            width: 100%;
            background: var(--text-color);
            color: white;
            padding: 1.2rem;
            border: none;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-size: 0.7rem;
            cursor: pointer;
            transition: 0.3s;
        }

        .submit-btn:hover {
            background: #444;
        }

        footer {
            padding: 4rem 5%;
            text-align: center;
            font-size: 0.65rem;
            letter-spacing: 2px;
            opacity: 0.6;
            line-height: 2;
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">Velvet Atelier</div>
        <div>AVAILABILITY: 15 SLOTS MONTHLY</div>
    </nav>

    <header>
        <div class="subtitle">Limited Edition Everlasting Blooms</div>
        <h1>The Sculptural Series</h1>
        <p style="font-size: 0.8rem; letter-spacing: 2px; margin-top: 2rem;">COMMONWEALTH, QUEZON CITY</p>
    </header>

    <div class="section-label" id="shop">The Collection</div>

    <div class="grid">
        <div class="product-card">
            <div class="product-image"></div> <div class="product-title">The Petit Bloom</div>
            <div class="product-price">150</div>
        </div>
        <div class="product-card">
            <div class="product-image"></div> <div class="product-title">The Atelier Signature</div>
            <div class="product-price">From 500</div>
        </div>
        <div class="product-card">
            <div class="product-image"></div> <div class="product-title">The Grande Heritage</div>
            <div class="product-price">2,500</div>
        </div>
    </div>

    <div class="section-label">Bespoke Inquiry</div>
    
    <div class="form-container">
        <form action="https://formspree.io/f/YOUR_ID" method="POST"> <input type="text" name="name" placeholder="FULL NAME" required>
            <input type="email" name="email" placeholder="EMAIL ADDRESS" required>
            <select name="series">
                <option value="" disabled selected>SELECT A SERIES</option>
                <option value="sculptural">THE SCULPTURAL SERIES</option>
                <option value="prosperity">THE PROSPERITY SERIES (MONEY)</option>
                <option value="heritage">THE GRANDE HERITAGE</option>
            </select>
            <textarea name="message" rows="4" placeholder="TELL US THE STORY BEHIND YOUR PIECE"></textarea>
            <button type="submit" class="submit-btn">Request Commission</button>
        </form>
    </div>

    <footer>
        VELVET ATELIER &copy; 2026<br>
        COMMONWEALTH, QUEZON CITY<br>
        HANDCRAFTED FOR THE PERMANENT.
    </footer>

</body>
</html>
